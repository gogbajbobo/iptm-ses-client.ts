export enum ActionTypes {
    LOGIN = 'LOGIN',
    LOGOUT = 'LOGOUT',
}

export enum GetterTypes {
    USER = 'USER',
    ACCESS_TOKEN = 'ACCESS_TOKEN'
}

export enum MutationTypes {
    RESET = 'RESET',
    LOGIN = 'LOGIN',
    LOGOUT = 'LOGOUT',
}
